# Support package and parallel NTD continuation — 2026-09-24

## Delivery boundary

The active product slice remains the reusable `concrete.slab.install` Support flow:
admitted evidence, candidate/fact review, declared Support scope, deterministic requirement
evaluation, and a register-first ID package containing the qualified AOSR PDF plus an editable
DOCX companion. OZERO was not used. The NTD stream is an independent persistent-platform
background workload and is not a prerequisite for this Support delivery.

`ProductReady=false`. This slice does not change the Tender, Audit, or Restoration denominators.

## Support result

Candidate source revision: `7277c637011cf5426b9f81495bd0d20a7f245008`.

The authenticated disposable Product Application flow uses the exact official source artifact
for Minstroy Order 344/pr and downloads a deterministic ZIP whose first member is the editable
register. The accepted qualification artifact is outside Git:

`/Users/oleg/.asd-kontur/qualification/support-browser-2d492c5-official/id-package-01a0d20c-0804-7988-91af-8378e7a6fe5d-v4.zip`

Archive SHA-256: `2ce2dcb828cdd2f6c6b2f661f258d4a72448d6faf5b460771dd0b7b9d950f4de`.

The archive contains the register DOCX/CSV, qualified finalized AOSR PDF, editable AOSR DOCX,
consistency report, evidence and missing-input schedules, manifest, and explicit incomplete
status. The package remains incomplete because the executive scheme and material-quality
documents are missing and the practice-only control attachment is blocked. No execution fact,
signature, measurement, or test result is inferred.

The AOSR PDF and DOCX carry the same checked representative values and source-bound template
version `344pr-2023@1.0.0`. The editable DOCX remains `template_candidate`; it is not promoted to
print-qualified output. The qualified PDF remains the authoritative printable representation.

Generated DOCX validation now permits content-driven pagination beyond the official four-page
template while rejecting output shorter than the form minimum. Browser acceptance now checks the
downloaded ZIP itself for register-first ordering and the editable AOSR member whenever the exact
official source is selected.

### Renderer evidence

- ONLYOFFICE `x2t` previously failed on a minimal DOCX and produced no qualified output.
- The canonical local DOCX renderer cannot run on this host: its `pdf2image` dependency and
  bundled LibreOffice executable are absent.
- The installed signed Apple Pages application did not complete a bounded scripted import/export
  attempt; no PDF was emitted and the attempt was terminated without changing the DOCX.
- Quick Look renders a readable first-page preview, but it is not all-page print qualification.

Therefore editable structure, reopening, semantic content, deterministic export, and download are
qualified; all-page independent DOCX print rendering remains blocked.

## Release and recovery

Exact-SHA CI run `35963850187` passed Python/PostgreSQL, browser, dependency/security, license,
secret, formatting, lint, type, OpenAPI and frontend gates for `7277c63`:

`https://github.com/yamazaki1711/asd-kontur/actions/runs/35963850187`

The public API/frontend and document worker remain on their prior releases with database migration
`0064_pit_observation_disposition_receipts`. Public Support activation is intentionally fail-closed:

1. no login/member technical identity exists for `asd_support_service`;
2. no production-approved `concrete.slab.install` work-type catalog entry exists;
3. no real human `support.scope.configure` professional grant exists;
4. editable DOCX all-page print qualification is unresolved.

The code does not require owner credentials as a substitute writer and synthetic qualification
grants remain confined to disposable databases.

A consistent pre-0065 database dump, object manifest, and previous API/worker plists are stored at:

`/Users/oleg/.asd-kontur/public-demo/backups/pre-0065-support-7277c63-20260924T061951Z`

The dump was restored into a disposable database and upgraded from 0064 to
`0065_project_field_candidate_bridge`. The restored copy retained 14 workspaces, 51 source
versions, and 1,633 NTD semantic rows. The disposable database was removed after verification.
Executable rollback alone is not sufficient after migration; the preserved 0064 dump/object
manifest is the recovery boundary until a forward-compatible public activation is approved.

## Parallel local-Qwen NTD stream

The deployed NTD worker release is
`/Users/oleg/.asd-kontur/public-demo/releases/20260924-7277c63-ntd-worker` at exact SHA
`7277c637011cf5426b9f81495bd0d20a7f245008`. It uses local
`Qwen3.8-27B-MLX-8bit` through the single local MLX runtime and is supervised by
launchd label `ru.asd-kontur.spine.ntd-worker`. The prior plist is preserved at
`/Users/oleg/.asd-kontur/public-demo/launchd-backups/20260924T1825-ntd-2b465f7`.
The selection policy was expanded only for explicit normative language missed by v1 (`не менее`,
`не более`, `запрещается`, `разрешается`, `обязан*`, `надлежит`). It does not enqueue all short,
multi-page, or non-modal fragments and does not promote model output to normative authority.

The job input now records eligibility policy/class. A bounded 1,600-token generation ceiling and
automatic retry for an immutable `qwen_semantic_response_output_exhausted` attempt are implemented
at `7277c63`. Accepted semantic rows are reused; OCR and prior model results are not replayed.

During this continuation an authenticated real-Qwen consultant request completed while the NTD
backlog was active. The then-running NTD job completed at `18:09:41+12`; the next NTD job was
claimed at `18:09:44+12`, and the browser consultant flow passed in 45.8 seconds. This demonstrates
foreground service at a job boundary and automatic background resumption without a second heavy
Metal process. It does not establish preemption inside a long generation; the non-preemptible bound
remains the current request timeout.

Measured scope at the start of this continuation was 262 succeeded / 0 failed local-profile jobs
and 1,612 total semantic rows. At `2026-09-24T18:25:46+12:00`, after the evidence-bound selector
expansion, the same scope was 294 succeeded / 0 failed, 1 running, 20 queued, and 1,644 semantic
rows. A rejected output-exhaustion attempt remains immutable; its job was safely requeued for a
second bounded attempt after the release was switched at a committed job boundary. These are job
and semantic-row denominators, not unique normative documents or qualified rules.

## Authority decisions still required

The production owner must separately approve a production work-type catalog version and grant a
real qualified human the Support scope capability. Operations must provision a least-privilege
login member of `asd_support_service` and supply `ASD_SUPPORT_COMMAND_DATABASE_URL`. These actions
must remain distinct from software qualification and do not authorize signatures or execution
facts.

The next single product step is controlled public activation of the exact candidate release at
migration 0065 after those technical and professional decisions, followed by the authenticated
real-authority Support scope → package → editable download run. The NTD worker continues
independently under launchd.
