# Contract Pack v2.5

Additive readiness evidence for `INDUSTRIAL-INTAKE-PROJECT-UNDERSTANDING-01`.
It preserves the 143-capability denominator and records all 17 `intake.*` and
12 `project-understanding.*` outcomes without rewriting earlier decisions.

Binary corpus files are generated outside Git by
`tools/build_industrial_intake_synthetic_corpus.py`; the fixture pins its
logical denominator and reproducibility contract.
