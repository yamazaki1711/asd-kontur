# Contract Pack v2.7

Additive professional-assistant contract for the current
`PILOT-USABLE-END-TO-END-01` delivery. It preserves the 143-capability product
denominator and every Contract Pack v2.6 pilot decision.

The pack requires one workspace-scoped assistant shared by all four modes,
real local `Qwen3.8-27B` inference, bounded access through the version-pinned
Knowledge Gateway, exact interactive sources, durable conversations and an
explicit unavailable outcome. The fixture remains pending until external
browser acceptance succeeds on the exact deployed merge commit.
