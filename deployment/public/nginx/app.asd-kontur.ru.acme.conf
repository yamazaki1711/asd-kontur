server {
    listen 80;
    server_name app.asd-kontur.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        default_type application/json;
        return 503 '{"detail":"tls_not_activated"}';
    }
}
